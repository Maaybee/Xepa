# O expo-router arrasta dependências web (Radix) que declaram peer de react-dom
# com um intervalo mais frouxo que o react fixado pelo SDK. O próprio instalador
# do Expo já resolve assim; fixar aqui deixa `npm install` reprodutível.
legacy-peer-deps=true
